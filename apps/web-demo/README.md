# @cheetah-media/web-demo

Reference web player application for Cheetah Media Engine.

## Scripts

- `pnpm dev` – start Vite dev server
- `pnpm build` – build production bundle
- `pnpm typecheck` – run TypeScript check
